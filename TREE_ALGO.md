# Алгоритм процедурных деревьев Watabou Village Generator

Восстановлен из минифицированного `Village.js` (https://watabou.github.io/village-generator/,
компилят Haxe → JS; имена классов сохранены в `__name__`). Ключевые классы:

- `com.watabou.village.mapping.TreeType` — генерация формы кроны и штриховки
  (`getCloudCrown` / `getPineCrown` / `getPalmCrown`, `drawShaded1/2/4`);
- `com.watabou.village.utils.Bloater` — «раздувание» полигона в облачный контур
  (`bloat`, `extrude`, `extrudeEx`);
- `com.watabou.village.utils.Spiker` — колючий/пальмовый контур (`simple`, `bent`);
- `com.watabou.village.mapping.ForestView` — отрисовка (`drawTree`,
  `drawLongShadow`, `drawSimpleShadow`, `drawConiferShadow`), кэш крон.

Три формы дерева (`w.TREE_SHAPE = ["Cotton", "Conifer", "Palm"]`, дефолт **Cotton**):

| Форма | Крона | Контур | Внутренние кольца | Штриховка |
|---|---|---|---|---|
| Cotton (cloud) | 12 точек | `Bloater.bloat` | `BALL_BANDS2 = [0.8, 0.5]` | `drawShaded1` |
| Conifer (pine) | 16 точек | `Spiker.simple` | `[0.6, 0.2]` или `[0.7, 0.4, 0.1]` | `drawShaded2` |
| Palm | 12 точек | `Spiker.bent` | `[0.7, 0.3]` | `drawShaded4` |

В QWE реализованы все три (`TreeShape` в `src/map/trees.rs`, переключается
панелью «Trees»), каждая — со своей процедурой штриховки; дальше подробно
разобран **Cotton** — дефолт и то, что на скриншоте. Отличия остальных — в §9.
QWE берёт `[0.7, 0.4, 0.1]` для хвои всегда: `CONE_BANDS2` у watabou достаётся
деревьям радиусом меньше 6, а здесь геометрия строится в единичном радиусе.

## 0. RNG

Везде один глобальный ГПСЧ — минимальный стандартный генератор Лемера
(Park–Miller), из него собираются «гауссоподобные» значения суммированием:

```js
// seed = 48271 * seed % 2147483647;  uniform = seed / 2147483647  ∈ (0, 1)
```

```rust
struct Lcg(u32);
impl Lcg {
    fn next_f32(&mut self) -> f32 {
        self.0 = ((u64::from(self.0) * 48271) % 0x7FFF_FFFF) as u32;
        self.0 as f32 / 2_147_483_647.0
    }
    /// Среднее трёх uniform — колокол на (0,1) со средним 0.5.
    fn gauss3(&mut self) -> f32 { (self.next_f32() + self.next_f32() + self.next_f32()) / 3.0 }
    /// Сумма четырёх uniform / 2 − 1 — колокол на (−1,1) со средним 0.
    fn bell4(&mut self) -> f32 {
        (self.next_f32() + self.next_f32() + self.next_f32() + self.next_f32()) / 2.0 - 1.0
    }
}
```

## 1. Базовый многоугольник кроны — `getCloudCrown(R)`

12 вершин по кругу; у каждой дрожат и угол, и радиус:

```text
для i в 0..12:
    angle_i  = 2π · (i + gauss3()) / 12          # джиттер угла, колоколом вокруг центра сектора
    radius_i = R · (1 − (4/12) · |bell4()|)      # радиус ∈ [⅔R, R], перекошен к R
    p_i      = polar(radius_i, angle_i)
```

`|bell4()|` — модуль колокола: чаще ~0, значит радиус чаще ~R и редко проседает
до ⅔R. Получается слегка мятый, почти выпуклый 12-угольник.

## 2. «Раздувание» в облако — `Bloater.bloat(points, k)`

Параметр лепестка: `k = R · sin(3π/12) = R · sin 45° ≈ 0.707·R`.

Каждое ребро рекурсивно делится: середина выталкивается **наружу** по
перпендикуляру, пока сегменты не станут короче `0.1·k`:

```js
// extrude(a, b, k):
//   d = a − b;  ratio = |d| / k
//   если ratio ≤ 0.1 → null (стоп)
//   n = (−d.y, d.x)                       // перпендикуляр (наружу при CCW-обходе)
//   f = 0.5 · min(ratio, 1)
//   вернуть midpoint(a, b) + n·f          // |смещение| = f·|d|
// extrudeEx(a, b, k):
//   m = extrude(a, b, k)
//   если m == null → [a]
//   иначе → extrudeEx(a, m, k) ++ extrudeEx(m, b, k)
// bloat(poly, k) = конкатенация extrudeEx по всем рёбрам (замкнуто)
```

```rust
fn extrude_edge(a: Vec2, b: Vec2, lobe: f32, out: &mut Vec<Vec2>) {
    let d = a - b;
    let ratio = d.length() / lobe;
    if ratio <= 0.1 {
        out.push(a);
        return;
    }
    let mid = a.midpoint(b) + Vec2::new(-d.y, d.x) * (0.5 * ratio.min(1.0));
    extrude_edge(a, mid, lobe, out);
    extrude_edge(mid, b, lobe, out);
}
```

Первый уровень рекурсии даёт по горбу на ребро (12 лепестков), следующие —
мелкие горбики на горбах: фирменный «кучевой» контур. Итог — плотная ломаная
~150–400 точек; кривых нет (SVG-экспорт подтверждает: только `L`-сегменты).

## 3. Внутренние кольца деталей — `BALL_BANDS2 = [0.8, 0.5]`

Тот же базовый 12-угольник, уменьшенный и сдвинутый «вверх» (к свету), раздутый
с **увеличенным относительным** лепестком (`k/scale` — фестоны крупнее
относительно кольца):

```text
для (n, scale) в enumerate([0.8, 0.5]):
    ring     = base · scale, сдвиг на (0, вверх · (n+1) · 0.15·R)
    band_n   = bloat(ring, k / scale)
    weight_n = 3 · scale²          # 1.92 для 0.8, 0.75 для 0.5
```

(«Вверх» — в экранных координатах y-вниз это `(0, −…)`; в Bevy y-вверх — `(0, +…)`.)

## 4. Отрисовка одного дерева — `ForestView.drawTree(g, center, R, color, thicket)`

Порядок слоёв:

1. **Крона**: заливка цветом дерева (§5), обводка `strokeNormal = 1` мировой
   единицы цветом чернил `colorInk = #010206`. Контур — `outer` из шага 2.
2. **Тень** — в отдельный слой-канвас ПОД всеми кронами (§6).
3. **Штриховка** — кольца из шага 3, штрих `strokeThin = 0.5` цветом чернил,
   **пунктиром** через `drawShaded1`:

```js
// drawShaded1(g, ring, c, w):  c — перпендикуляр к направлению тени
// для каждого ребра (prev → cur) замкнутого кольца:
//   dir = normalize(cur − prev)
//   p   = w · (0.5 + 0.5 · dot(c, dir))     // вероятность нарисовать ребро
//   если rand() < p → g.moveTo(prev); g.lineTo(cur)
```

Рёбра, чьё направление обхода совпадает с `c`, рисуются почти всегда, на
противоположной стороне кольца — почти никогда: дуги-штрихи скапливаются на
одной (теневой) стороне. Кольцо 0.8 (вес 1.92) прорисовывается почти целиком с
разрывами; кольцо 0.5 (вес 0.75) — редкими короткими дугами.

## 5. Цвет кроны

Базовый цвет `w.colorTree = #496664`; каждому дереву — свой множитель яркости:

```text
color = base · 2^(treeVariance · bell3)      # treeVariance = 0.2, bell3 ∈ (−1, 1)
       ⇒ множитель ∈ [0.87, 1.15] на все каналы RGB
```

## 6. Тень

`shadowDir = 30°` (на экране — вправо-вниз), `shadowLength = 1`,
`colorShadow = #9699AE`; слой теней рисуется под кронами в blend-режиме
**multiply** с лёгким blur — поэтому пересекающиеся тени не двоятся.

Случайный `h ∈ [0.4, 1.2]`; для Cotton при `h · shadowLength > 0.5` (~87%
случаев) — **длинная тень** `drawLongShadow(outer, center, R, d = 0.8)`:
силуэт кроны, растянутый вдоль направления тени:

```text
stretch = 1 + 0.5 · shadowLength · d = 1.4
p′ = rot(+θ) · ( diag(stretch, 1) · (rot(−θ)·p + (R, 0)) + (−R·(1 − shadowLength/4), 0) )
   # θ — угол тени; сдвиг центра ≈ 0.65·R вдоль тени, охват от −0.75·R до 2.05·R
```

Кэшируется по контуру. Иначе — простая тень: тот же силуэт со сдвигом
`h · R · shadowVec`. Conifer вместо этого рисует **веер** убывающих силуэтов
вдоль тени — `drawConiferShadow`:

```text
tip   = 3·h·R·shadowVec                       # дальний конец конуса
треугольник [ +R·perp(shadowDir), −R·perp(shadowDir), tip ]   # ствол конуса
count = ceil(h · shadowLength · 3)
для k в 0..count:  силуэт·(1 − k/count), сдвинутый на tip·(k+1)/(count+1)
```

`h` разыгрывается **на дерево**, поэтому у соседних деревьев тени разной длины.

## 7. Кэш геометрии

`drawTree` кэширует результат `getCrown(R)` по ключу `⌊10·R⌋` — одна крона на
дециль радиуса; деревья близкого размера делят геометрию, а разнообразие дают
непрерывный разброс радиусов, свой цвет и своя тень.

## 8. Перенос в QWE (Bevy, y-вверх)

- Генерация в единичном радиусе, масштаб — `Transform::scale = radius`
  (форма масштабо-инвариантна: `k ∝ R`).
- Ось y перевёрнута: сдвиг колец — `(0, +0.15·(n+1))`; направление тени —
  `(cos −30°, sin −30°) = (0.866, −0.5)`; вектор штриховки для CCW-колец —
  `perp(shadow_dir) = (0.5, 0.866)`.
- Тень — силуэт с аффинным растяжением из §6, но альфа-блендинг вместо
  multiply (тёмный полупрозрачный цвет), слой `Z_TREE_SHADOW` под кронами.
- Заливка (earcut), обводка и штрихи — один вершинно-раскрашенный меш на
  вариант (`MeshBuilder`); `TREE_VARIANTS` вариантов, каждому дереву — вариант,
  квантованный оттенок (§5) и масштаб по радиусу.
- Обводка — **лента с miter-стыками** (`MeshBuilder::push_stroke`), а не квад
  на сегмент: контур после `bloat` содержит рёбра короче толщины штриха, и
  наивные продлённые квады торчали шипами. `MITER_LIMIT = 1.5` срезает стык на
  почти встречных рёбрах — на остриях (шипы хвои, впадины между фестонами)
  срез оставляет снаружи клин пустоты, и он читается как **разрыв контура**;
  такие стыки дополнительно закрываются веером в полуширину штриха, то есть
  остриё выходит скруглённым, как при round-стыках Flash у watabou.
- Штрихи колец рисуются не по одному куску, а **связными дугами**: соседние
  выбранные куски склеиваются (включая склейку через начало кольца), иначе
  каждый рисуется своими торцами и кольцо распадается на зубцы. У `drawShaded1`
  отбрасываются дуги короче толщины штриха — такая при зуме уже не штрих, а
  квадратик; всё, что длиннее, рисуется, иначе кольцо читается как рваное.
- Стиль (`TreeStyle`) — ресурс: листва, цвет чернил, `variance`, форма. Правка
  из панели «Trees» пересобирает кроны (`rebuild_trees`).

## 9. Conifer и Palm

Отличаются от Cotton четырьмя параметрами; остальной код общий:

| | Cotton | Conifer | Palm |
|---|---|---|---|
| Вершин базы | 12 | 16 | 12 |
| Джиттер радиуса | 4/12 | 0.25 | 4/12 |
| Контур | `bloat` | `Spiker::simple` | `Spiker::bent` |
| Кольца (масштаб → вес) | 0.8, 0.5 → `3s²` | 0.7, 0.4, 0.1 → `0.5+s` | 0.7, 0.3 → `3s` |
| `lobe` колец | `k/scale` | `k` | `k` |

`Spiker::simple` — вместо горба над ребром ставится один **шип**: наружная
нормаль длиной `|ребро|·sqrt(|ребро|/k)` из середины ребра. Получается колючая
звезда — ель сверху.

```rust
fn spike_vector(from: Vec2, to: Vec2, lobe: f32) -> Vec2 {
    let edge = to - from;
    let scaled = edge * (edge.length() / lobe).sqrt();
    Vec2::new(scaled.y, -scaled.x)   // наружная нормаль при CCW-обходе
}
```

`Spiker::bent` — тот же шип плюс две опорные точки (до и после вершины),
сдвинутые на `0.1` по нормали и на `0.1·|шип|` к свету: лист изгибается, а не
торчит иглой.

Сдвиг колец к свету — константа **формы**, а не номера кольца:
`−(n+1)·R·0.15` у Cotton, `·0.12` у Conifer, `·0.1` у Palm.

### Отступление: залипшие вырезы контура ели

`Spiker::simple` иногда ставит два шипа почти вплотную, и вырез между ними
выходит уже двух ширин обводки (`TREE_OUTLINE_STROKE` = 0.12 радиуса). Обводка
заливает его целиком: просвет между стенками исчезает, пара шипов читается
одним чёрным горбом с иглой внутрь кроны, и та же клякса уходит в веер тени. На
12 вариантах QWE таких вырезов тринадцать из 192.

У watabou обводка фиксированной ширины в мировых единицах, а город смотрят
издалека — вырождение не видно. Здесь контур ели строит `cone_outline`: у каждой
впадины считается устье `2·min(плечо)·sin(φ/2)` и глубина (отступ от хорды между
шипами), и если устье меньше `2·штриха` при глубине от `1.5·штриха`, вырез
раскрывается.

Раскрывается **на базовом 16-угольнике**: устье узко потому, что шипы над двумя
соседними рёбрами почти параллельны, а расходятся они тем сильнее, чем круче
излом базы между этими рёбрами. Виновная вершина базы сдвигается поперёк хорды
своих соседей на наименьшее смещение, которое вырез раскрывает — на 12 вариантах
хватает шести сотых радиуса. Все 32 точки контура остаются на месте, вылет шипов
(1.31–1.55) и остроты (51–88°) не меняются.

Убирать шип нельзя ни с готового контура — у соседнего база расширяется вдвое и
остриё тупеет до 107°; ни с базы — объединённое ребро вдвое длиннее, а шип
растёт как `len^1.5`, и вылет уходит к 1.9 радиуса. Обе попытки давали ровно тот
«горб», ради которого всё и затевалось.

Правка идёт **только по контуру** — кольца штриховки строятся из нетронутой
базы, потому что держатся на чередовании «база-шип», по которому `drawShaded2`
шагает через два. Cotton и Palm мимо: у облака самый глубокий вырез 0.061 при
пороге 0.18 (раскрывать нечего), у пальмы тонкие листья — по замыслу.

### Штриховка колец — своя у каждой формы

Кольцо после `Spiker` чередует вершины базы и кончики шипов, и штрихуется оно
не по одному ребру, а кусками длиной в шип:

```js
// drawShaded2 (Conifer): шаг 2, кусок база→шип→база
//   dir = normalize(ring[h+2] − ring[h])        // хорда базы, не ребро шипа
//   если w·(0.5 + 0.5·dot(c, dir)) > 0.5 → moveTo(ring[h]); lineTo(ring[h+1]); lineTo(ring[h+2])
// drawShaded4 (Palm): шаг 4, лист целиком (5 точек), порог через ГПСЧ как в drawShaded1
```

`drawShaded2` **без ГПСЧ**: условие либо выполнено, либо нет. Поэтому соседние
шевроны смыкаются в сплошную ломаную-«этаж» на теневой стороне, а светлая
остаётся чистой. Одно отступление от оригинала: джиттер угла иногда качает
ровно одну хорду посреди прогона за порог (на 12 вариантах QWE таких колец
пять), и «этаж» распадается надвое. У watabou дырка остаётся — при его сорока
пикселях на дерево её не видно; здесь дырки длиной в один шеврон закрываются
(`close_single_gaps`), иначе на зуме это ровно тот разрыв, ради которого всё и
затевалось — при лотерее по ребру «этаж» рассыпался бы на штрихи, и часть
их вылезала бы против тени. Веса `0.5 + scale` (1.2 / 0.9 / 0.6) дают дуги
примерно в 190°, 167° и 96°; последняя — кольцо `0.1`, поднятое на `3·0.12` к
макушке, — и есть **верхушка ели**.
